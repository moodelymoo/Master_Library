package Scrabble.GameRuntime;

public class GameStateController {

    public GameStateController() {

        // setup

        // assume default setup worked as intended

        // game loop (round)
            // check no prior challenge turn skip applied

            // p1 turn
            // must play tiles

            // p2 can challenge

                // if no challenge
                    // refill hand

                // else check word
                    //if word exists
                        // skip p2 turn

                    // else
                        // remove tiles from board to p1 hand

        // go to round start and increment player e.g. p1 -> p2 ect...
    }


}
